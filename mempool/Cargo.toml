[package]
name = "time-mempool"
version = "0.1.0"
edition = "2021"

[dependencies]
time-core = { path = "../core" }
tokio = { version = "1.41", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"

[dev-dependencies]
